export type GlobalStats = {
  damage: number;
  reloadTime: number;
  cadency: number;
  range: number;
  magazineSize: number;
};

export type GlobalStatsJson = {
  damage: number[];
  reloadTime: number[];
  cadency: number[];
  range: number[];
  magazineSize: number[];
};
